Bugs:
1. addFront does add a value, but it doesn't add to the front of the list.
2. removeBack returns false even if the size is > 0.
3. removeFront returns false even if the size is > 0.
4. isEmpty always returns true.
